Quest 112 verification
=======================

Branch: codex/quest-112-sessions-page

Automated checks (2026-08-01, after PR review fixes):
- `gofmt` and `git diff --check`: passed.
- `go build -buildvcs=false ./...`: exit 0.
- `go vet ./...`: exit 0.
- `go test ./...`: exit 0. Final package results included:
  `ok github.com/janiorvalle/tokenomnom/internal/tui`
  `ok github.com/janiorvalle/tokenomnom/internal/tui/pages`
  `ok github.com/janiorvalle/tokenomnom/internal/history/store`
  `ok github.com/janiorvalle/tokenomnom/internal/cli`
- Autoreview: `autoreview clean: no accepted/actionable findings reported`.
- `GOFLAGS=-buildvcs=false make verify`: exit 0. This covered build, vet, all
  tests, GoReleaser config/snapshot validation, and the install smoke test;
  the smoke test ended with matching `tokenomnom version` and `nomnom version`
  output for the snapshot commit.

Review follow-up:
- Removed the discarded 500-row catalog read from every history load.
- Added `dashboardSessionCache`, keyed by provider, date range, project filter,
  and cursor; selection/detail movement now reuses the loaded page while sync,
  pagination, and filter changes refresh it.
- `TestDashboardSessionCacheRefreshesWhenQueryChanges` passed, including the
  sync refresh boundary; autoreview found no accepted/actionable findings.

Stored history receipt from the fixture index:
- session_id: `ses_a4c7d0c737ac989e0f07bf5c56e4288d`
- provider/project: `codex` / `tokenomnom`
- logical_prompt_count/occurrence_count: `1` / `1`
- preferred_retrieval_source: `provider-live`
- preview: `review the sessions page flow`

Keyboard-only PTY verification used tmux with the populated fixture:
- `5` selected the Sessions page; the rendered row showed `2026-08-01`, `codex`,
  `tokenomnom`, `1`, and `review the sessions page flow`.
- `Enter` rendered `SESSION DETAIL` with `FIRST PROMPT`, `OVERVIEW`, and
  `PROVENANCE`; the wide frame showed `esc back to sessions`.
- Resizing to 80x18 and pressing `Down` rendered `↑ more above` and
  `↓ more below`, proving compact-terminal detail scrolling.
- `Home` then `Escape` returned to the Sessions list; `q` exited.

Empty-index PTY verification rendered these exact actionable lines:
- `No history index is available.`
- `Run tokenomnom history index to browse sessions.`

The repository's linked worktree makes plain `go build ./...` attempt
`git status` from `/Users/janiorvalle` and fail VCS stamping with exit 128;
the compile gate therefore uses Go's documented `-buildvcs=false` workaround.
